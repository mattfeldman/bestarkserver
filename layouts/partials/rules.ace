.ui.vertical.stripe.segment#rules
  h1.ui.header.container
    i.warning.sign.icon
    .content Rules
      .sub.header Play nice
  .ui.container
    .ui.relaxed.huge.list
      {{ range .Site.Data.ark.data.rules }}
        .item
          .content
            .ui.header {{ .title }}
            {{ .details }}
      {{ end }}